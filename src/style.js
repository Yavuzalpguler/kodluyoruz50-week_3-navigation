import { StyleSheet } from 'react-native'

const styles = {
    postItem: StyleSheet.create({
        container: {
            backgroundColor: '#eceff1',
            padding: 5
        },
        title: {
            fontWeight: 'bold',
            fontSize: 17
        }
    })
}

export default styles